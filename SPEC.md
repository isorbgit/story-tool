# 가면을 벗다 — 세계관/시나리오 맵 툴 기획 명세

> 문서 버전 v0.8 / 2026-07-30
> 작성: 이솝 + Claude
>
> v0.8 — **배포 확정** ([1.5](#15-배포)) — GitHub Pages. 아이패드 실측 완료
> v0.7 — 단일 파일 빌드([3.6](#36-단일-파일-빌드)) — 아이패드 반출용
> v0.6 — **연대 체계 확정** ([6장](#6-시점-필드-설계)) — 1년 7달 199일. 타임라인 해금
> v0.5 — 마크다운 내보내기([3.5](#35-마크다운-내보내기)), Phase 4 착수
> v0.4 — 스키마 편집기([5.3.1](#531-스키마-편집기)), 적용 전 검증·영향 분석([5.3.2](#532-적용-전-두-가지를-따로-본다))
> v0.3 — 화면 배치·스플리터([9.1](#91-화면-배치))
> v0.2 — Firefox 153 실측 반영: OPFS 출처 격리([1.3](#13-실행-환경)),
> 시작 화면 표시 조건([3.0](#30-저장-어댑터))

---

## 1. 개요

### 1.1 목적
인디 게임 「가면을 벗다」(Inner Flower Game Studio)의 세계관 설정과 시나리오를
노드 그래프 형태로 정리·관리한다. 산발적으로 흩어진 설정 문서를
**요소 간 상관관계가 시각적으로 드러나는 단일 데이터베이스**로 통합하는 것이 1차 목표다.

### 1.2 설계 모티브
언리얼 엔진 **블루프린트 에디터**의 UI/UX.
- 노드 + 소켓 + 베지어 연결선
- 타입별 색상 코딩, 타입 불일치 연결 거부
- 좌→우 흐름, 우측 디테일 패널

블루프린트와 의도적으로 다른 점은 [4장](#4-캔버스-동작-규칙) 참조.

### 1.3 실행 환경

단일 HTML + JS (빌드 도구 없음). 서버·DB 없음. **PC / iOS 양쪽에서 동작**하며,
한쪽에서 만든 데이터를 반대쪽에서 열 수 있어야 한다.

**실측 결과 (2026-07-30, Chrome 151 / Firefox 153 / 아이패드 Safari):**

| 환경 | `showDirectoryPicker` | OPFS 실제 쓰기 | IndexedDB | 고르는 어댑터 |
|---|---|---|---|---|
| Chrome `file://` | **있음** | SecurityError | OK | **FSA** — 폴더 저장 |
| Chrome localhost | 있음 | OK | OK | **FSA** — 폴더 저장 |
| Firefox `file://` | 없음 | **OK, 재시작해도 남음** | OK | **OPFS** |
| Firefox localhost | 없음 | OK | OK | **OPFS** |
| **아이패드 Safari · https** | 없음 (전부 WebKit) | **OK** | — | **OPFS** |
| 아이패드 Safari · 평문 http(LAN) | 없음 | 막힘 (보안 컨텍스트 아님) | — | 메모리(임시) |

> **아이패드는 https 로 열면 정상 동작한다** (2026-07-30 확인).
> 열기 · 편집 · 저장 · 다시 열어 불러오기까지 끝까지 확인했다.
> 배포 위치는 [1.5](#15-배포), 미검증으로 남은 경로는 [3.6](#36-단일-파일-빌드) 참조.

> **"`file://` 면 저장 못 한다" 는 사실이 아니다.** Chrome 은 `file://` 에서도
> 폴더 선택이 되고, Firefox 는 `file://` 에서도 OPFS 가 되며 브라우저를 껐다 켜도
> 데이터가 남는다(할당량 10GB 확인). 반대로 Chrome 의 OPFS 는 `file://` 에서만 막힌다.
>
> 그래서 어댑터는 **프로토콜이나 브라우저 이름으로 넘겨짚지 않고 실제로 써 보고 정한다**
> ([3.0](#30-저장-어댑터)).

| | 폴더 저장 (권장) | 브라우저 내부 저장 |
|---|---|---|
| 조건 | Chrome / Edge | 그 외 (Firefox, Safari, iOS) |
| 데이터 가시성 | 탐색기에서 보임, **git 관리 가능** | 안 보임 |
| 교환 | 양쪽 모두 단일 번들 파일 ([3.4](#34-교환-포맷)) | |

`serve.cmd` 를 더블클릭하면 `http://localhost:8777` 로 뜬다. PC 에서 쓰는 용도다.
**LAN 주소로 아이패드에서 접속하는 길은 막혔다** — 평문 http 는 보안 컨텍스트가 아니라
OPFS 가 통째로 막히고 임시 모드로 돈다. 아이패드는 [1.5](#15-배포)의 https 주소로 쓴다.

#### 주소가 바뀌면 저장소도 바뀐다 (OPFS 한정)

OPFS 는 **출처(origin)별로 격리**된다. 같은 파일을 열어도 주소가 다르면 다른 저장소다.
실측(2026-07-30, Firefox 153): `file://` 에서 노드 2개를 저장한 뒤 `http://localhost:8777`
로 열면 **노드 0개**. 데이터가 사라진 게 아니라 다른 서랍을 연 것이다.

| 여는 방법 | 출처 | 저장소 |
|---|---|---|
| `index.html` 더블클릭 | `file://` | A |
| `serve.cmd` → localhost | `http://localhost:8777` | B (A와 별개) |
| 배포 주소 ([1.5](#15-배포)) | `https://isorbgit.github.io` | C (아이패드가 쓰는 곳) |

- **하나를 정해서 계속 쓴다.** 섞어 쓰면 "작업이 날아갔다"로 보인다
- 주소를 옮길 때는 반드시 [내보내기] → [가져오기] ([3.4](#34-교환-포맷))
- 이 제약은 OPFS 에만 해당한다. 폴더 저장(FSA)은 실제 폴더를 가리키므로 주소와 무관하다
- 미검증: `file://` 에서 **프로젝트 폴더를 다른 경로로 옮겼을 때** 저장소가 유지되는지는
  확인하지 않았다. 폴더 이동 전에는 내보내 둘 것

### 1.4 렌더링 방식
| 요소 | 구현 |
|---|---|
| 노드 | HTML `div` (absolute positioning) |
| 연결선 | SVG `path` (cubic bezier) |
| 팬/줌 | 전체를 감싼 단일 wrapper의 `transform: translate() scale()` |
| 목표 규모 | 노드 500개 / 캔버스당 배치 80개 수준까지 무리 없음 |

### 1.5 배포

아이패드에서 쓰려면 https 가 필요하므로([1.3](#13-실행-환경)) GitHub Pages 로 서빙한다.

```
저장소   https://github.com/isorbgit/story-tool     (공개)
주소     https://isorbgit.github.io/story-tool/
갱신     git push  →  1~2분 뒤 반영
```

- 아이패드는 **Safari 로 열고 [홈 화면에 추가]** 해서 쓴다
- **올라가는 것은 앱 코드뿐이다.** `data/` · `backup/` 은 `.gitignore` 로 막았다.
  세계관 데이터는 기기 안(폴더 또는 OPFS)에만 있고 서버는 받지도 보관하지도 않는다
- `SPEC.md` 도 제외돼 있다. 작품 설정 예시가 들어 있어 공개 여부를 따로 판단한다.
  올리려면 `.gitignore` 에서 그 줄을 지운다
- 앱을 고쳐 push 해도 **아이패드에 쌓인 데이터는 그대로다.** 데이터는 코드가 아니라
  기기의 OPFS 에 있다

> **이 주소가 아이패드 저장소의 신원이다.** 저장소 이름을 바꾸거나 다른 곳으로 옮기면
> 그때까지 쌓인 데이터가 안 보인다. 옮길 일이 생기면 **먼저 [내보내기]로 번들을 빼둔다.**
> PC 와 아이패드도 서로 다른 저장소를 쓰므로, 양쪽을 오가는 통로는 번들뿐이다
> ([3.4](#34-교환-포맷)).

---

## 2. 데이터 아키텍처

### 2.1 핵심 원칙 — 3분리

```
노드(존재)  ──  엣지(사실)  ──  배치(표현)
   전역           전역          캔버스별
```

| 대상 | 저장 위치 | 이유 |
|---|---|---|
| **노드** | 전역 (`nodes.json`) | 인물 A는 세계에 한 명뿐 |
| **엣지** | 전역 (`edges.json`) | 두 인물의 관계는 세계관의 사실. 캔버스별로 두면 챕터마다 어긋난다 |
| **배치** | 캔버스별 (`canvases.json`) | 좌표는 보기 편의일 뿐 |

캔버스는 **"양쪽 노드가 모두 이 캔버스에 배치돼 있으면 선을 그린다"** 로만 동작한다.
캔버스가 엣지를 소유하지 않는다.

### 2.2 ID 규칙
사람이 읽을 수 있는 `prefix_slug` 형식. 랜덤 해시 금지.

| 타입 | prefix | 예시 |
|---|---|---|
| 사건 | `evt_` | `evt_mask_burning` |
| 인물 | `chr_` | `chr_lena` |
| 조직 | `org_` | `org_night_watch` |
| 물건/작품 | `itm_` | `itm_silver_mask` |
| 개념 | `cpt_` | `cpt_maskless_doctrine` |
| 장소 | `loc_` | `loc_old_quarter` |

- ID는 생성 후 **변경 불가**. 표시명(`name`)만 자유롭게 수정.
- 중복 slug 입력 시 `_2` 자동 부여 + 경고.

### 2.3 게임 연동 대비
export 포맷은 미정이나, 나중에 어떤 포맷이든 대응할 수 있도록 **지금 두 가지만** 확보한다.
1. 위의 읽을 수 있는 ID 체계
2. 모든 노드에 `gameKey` 필드 (기본값 빈 문자열) — 게임 내 데이터 테이블 키와 매칭용

---

## 3. 파일 구조

```
worldmap/
├─ index.html
├─ serve.cmd / serve.js   ← 로컬 서버 (의존성 없음, iOS·localhost 용)
├─ build.cmd / build.js   ← 단일 파일 빌드 ([3.6](#36-단일-파일-빌드))
├─ worldmap-single.html   ← 빌드 결과물. 생성물이므로 직접 고치지 않는다
├─ js/
├─ style.css
├─ data/
│   ├─ schema.json      ← 타입·소켓·필드 정의 (툴의 설정)
│   ├─ nodes.json
│   ├─ edges.json
│   └─ canvases.json
└─ backup/
    ├─ 20260728-1430/
    └─ ... (최근 10개 로테이션)
```

- 편집 후 **2초 디바운스 자동 저장**
- `backup/` 스냅샷은 **세션 첫 저장 · 이후 10분 간격 · 가져오기 직전**에만 뜬다.
  10개 초과 시 오래된 것 삭제
- 4개 파일로 분리한 이유: git diff 가독성

> 백업을 "저장할 때마다" 뜨면 2초 디바운스와 맞물려 10개 슬롯이 편집 20초치로
> 가득 찬다. 되돌리고 싶은 시점은 대개 그보다 훨씬 뒤라 그 백업은 쓸모가 없다.

**이 트리 구조는 PC·iOS 공통이다.** iOS에서는 같은 트리가 사용자 폴더 대신
OPFS 안에 만들어진다. 경로와 파일명이 같으므로 상위 로직은 환경을 구분하지 않는다.

### 3.0 저장 어댑터

저장 계층은 인터페이스 하나로 추상화하고, 환경에 따라 구현체를 갈아끼운다.
상위 코드(노드 CRUD·자동저장·백업)는 어느 구현체가 붙었는지 모른다.

```js
// 모든 어댑터가 만족해야 하는 계약
{
  id,                       // 'fsa' | 'opfs' | 'memory'
  isAvailable(),            // 이 환경에서 쓸 수 있는가
  connect(),                // 폴더 선택 / OPFS 루트 획득
  readAll(),                // → { schema, nodes, edges, canvases }
  writeFile(name, text),
  listBackups(), writeBackup(stamp, files), deleteBackup(stamp),
  canRotateBackup,          // OPFS는 true, 용량 제약만 다름
  isUserVisible             // 탐색기에서 보이는가 (UI 안내 문구 분기용)
}
```

| 구현체 | 사용 환경 | 비고 |
|---|---|---|
| `FsaAdapter` | 데스크톱 크로미움 | 폴더 핸들을 IndexedDB에 저장 → 재방문 시 자동 재연결 |
| `OpfsAdapter` | iOS, 그 외 FSA 미지원 브라우저 | 사용자에게 안 보이므로 **내보내기 유도 UI 필수** |
| `MemoryAdapter` | 위 둘 다 실패 시 | 읽기 전용 데모. 새로고침하면 날아감을 명시 |

선택 순서: `FsaAdapter` → `OpfsAdapter` → `MemoryAdapter`.

**OPFS 는 API 존재 여부로 판정하지 않는다.** `navigator.storage.getDirectory` 가 있어도
실제로 쓰면 막히는 조합이 있다(Chrome `file://`). 그래서 `OpfsAdapter.probe()` 가
임시 파일을 실제로 쓰고 읽어 본 뒤 성공할 때만 채택하고, 실패하면 그 사유를 들고
`MemoryAdapter` 로 내려간다. 시작 화면의 진단 표가 이 결과를 그대로 보여준다.

#### 시작 화면을 언제 띄우는가

시작 화면은 **사용자에게 물어볼 것이 있을 때만** 띄운다. 물어볼 게 없는데 띄우면
매번 클릭 한 번을 강요하는 통행세가 된다.

| 어댑터 | 조건 | 동작 |
|---|---|---|
| `fsa` | 폴더 권한이 남아 있음 | 조용히 재연결 → 바로 앱 |
| `fsa` | 권한 없음 / 첫 실행 | 시작 화면 — **폴더를 골라야 하므로 사용자 제스처 필수** |
| `opfs` | 저장된 데이터 있음 | 조용히 열고 → 바로 앱 |
| `opfs` | 데이터 없음 (첫 실행) | 시작 화면 — 저장 위치가 안 보인다는 안내를 한 번은 읽혀야 한다 |
| `memory` | 항상 | 시작 화면 — **저장이 안 된다는 사실은 건너뛰면 안 된다** |

`opfs` 는 FSA 와 달리 권한 프롬프트가 없어 제스처 없이 열 수 있다. 그래서 재방문 시
Chrome 과 동일하게 곧장 앱으로 들어간다. "저장된 데이터가 있는가" 는 `nodes`/`edges`/
`canvases` 중 하나라도 파일이 있는지로 판정한다(`app.js` 의 `hasSavedData`).
첫 부팅에서 4개 파일을 모두 써넣으므로, 2회차부터는 항상 참이다.

> 실측(2026-07-30, Firefox 153 `file://`): 1회차 시작 화면 → 저장 → 브라우저 완전 종료
> → 2회차 시작 화면 없이 노드 2개 그대로 복원.

### 3.1 nodes.json

```json
{
  "chr_lena": {
    "type": "character",
    "name": "레나",
    "gameKey": "",
    "tags": ["주연", "1장"],
    "status": "confirmed",
    "reveal": "public",
    "impl": "done",
    "fields": {
      "alias": "은가면",
      "lifespan": "3기 981 ~ ",
      "summary": "...",
      "secret": "..."
    },
    "created": "2026-07-28T14:30:00+09:00",
    "updated": "2026-07-28T14:30:00+09:00"
  }
}
```

### 3.2 edges.json

```json
{
  "e_0001": {
    "from": "chr_lena",
    "fromSocket": "events",
    "to": "evt_mask_burning",
    "toSocket": "characters",
    "label": "주도",
    "note": "본인은 우발적이었다고 주장",
    "status": "confirmed"
  }
}
```

- 엣지는 **소켓 단위로 기록**한다. 어느 핀에 꽂혔는지가 관계의 의미다.
- 역방향은 저장하지 않는다. 조회 시 양방향 인덱스를 메모리에 구축.

### 3.3 canvases.json

```json
{
  "cv_ch1": {
    "name": "1장 — 가면 소각",
    "order": 1,
    "placements": {
      "evt_mask_burning": { "x": 400, "y": 200, "collapsed": false },
      "chr_lena":         { "x": 120, "y": 380, "collapsed": true }
    },
    "comments": [
      { "x": 80, "y": 150, "w": 600, "h": 400, "text": "발단부", "color": "#3a4" }
    ]
  }
}
```

### 3.4 교환 포맷

PC ↔ iOS 이동은 **단일 번들 파일**로 한다. OPFS는 사용자에게 보이지 않으므로
파일 4개를 개별로 주고받게 하면 실수가 난다.

```
worldmap-20260730-1107.wmap.json
```
```json
{
  "format": "wmap-bundle",
  "version": 1,
  "exportedAt": "2026-07-30T11:07:00+09:00",
  "exportedFrom": "fsa",
  "nodes": { }, "edges": { }, "canvases": { },
  "schema": { }
}
```

- 내보내기: 양쪽 모두 파일 1개 다운로드. iOS는 파일 앱/AirDrop/iCloud로 옮김
- 가져오기: PC는 FSA 또는 `<input type="file">`, iOS는 `<input type="file">`
- 가져오면 어댑터가 다시 4개 파일로 풀어 쓴다. **PC의 git diff 가독성은 유지된다**
- `schema`는 번들에 포함하되 가져오기 시 적용 여부를 따로 묻는다

#### 병합 규칙 (덮어쓰기 금지)

가져오기는 무조건 덮어쓰지 않는다. 노드·엣지의 `updated` 타임스탬프를 비교해
**요약을 먼저 보여주고 확인을 받는다.**

> 가져오기: `worldmap-20260730-1107.wmap.json`
> · 추가 **12** · 갱신 **4** · 변경 없음 **131**
> · **충돌 2** — 양쪽 모두 마지막 동기화 이후 수정됨
> [충돌 항목 보기] [취소] [가져오기]

- 충돌 항목은 **항목별로 이쪽/저쪽 선택**. 일괄 선택도 제공
- 삭제는 전파하지 않는다 (한쪽에서 지운 노드가 다른 쪽에 남아 있으면 "추가"로 취급).
  삭제 동기화는 툼스톤이 필요한데 그 복잡도를 지금 감당할 이유가 없다
- 가져오기 직전 자동으로 백업 스냅샷을 남긴다

### 3.5 마크다운 내보내기

번들([3.4](#34-교환-포맷))이 **이 툴로 돌아오기 위한 포맷**이라면, 마크다운은
**이 툴 없이 읽기 위한 포맷**이다. 목적이 다르므로 형태도 다르다.

```
export/markdown/
├─ index.md                       ← 타입별 목차
├─ character/chr_lena.md
├─ organization/org_night_watch.md
└─ event/evt_mask_burning.md
```

- **노드 하나당 문서 하나.** 연결은 상대 문서로 가는 **상대경로 링크**로 건다.
  Obsidian·GitHub 에서 그대로 걸어다닐 수 있어야 한다
- 프론트매터에 `id`/`type`/`gameKey`/플래그/타임스탬프를 싣는다. 문서만 보고도
  원본 노드를 되짚을 수 있어야 한다
- `markdown` 위젯 필드는 원문 그대로 통과시킨다. 표시명·라벨처럼 **문서가 아닌 값만**
  마크다운 특수문자를 이스케이프한다 (이름에 `*` 나 `[` 가 있으면 링크가 깨진다)

| 옵션 | 기본 | 쓰임 |
|---|---|---|
| 미공개(spoiler) 제외 | 안전 모드 상태를 따름 | 공유용 문서. 노드·필드·그리로 가던 링크까지 함께 뺀다 |
| 폐기(dropped) 제외 | 켬 | |
| 등장 캔버스 포함 | 켬 | |

**출력 위치는 저장 방식이 정한다.**

| 어댑터 | 출력 | 이유 |
|---|---|---|
| `fsa` | 폴더의 `export/markdown/` 에 직접 | 탐색기에서 보이고 git 으로 diff 가 된다 |
| `opfs` · `memory` | **ZIP 내려받기** | 폴더가 안 보이므로 써 봐야 꺼낼 수 없다([1.3](#13-실행-환경)) |

- 폴더에 쓸 때는 **먼저 `export/markdown/` 을 지우고 새로 쓴다.** 안 지우면 삭제된
  노드의 문서가 계속 남는다
- ZIP 은 무압축(STORE)으로 직접 만든다. 빌드 도구가 없어 라이브러리를 쓸 수 없고,
  문서는 텍스트라 압축이 아쉽지만 의존성 없이 도는 편이 낫다

### 3.6 단일 파일 빌드

`build.cmd` 를 더블클릭하면 `style.css` 와 `js/` 18개를 `index.html` 안에 인라인해
**`worldmap-single.html` 하나**로 합친다 (약 290KB).

```
index.html + style.css + js/ 18개  ──build.js──▶  worldmap-single.html
```

**아이패드는 이제 https 로 쓰므로([1.5](#15-배포)) 평소에는 필요 없다.** 인터넷 없이
파일 하나만 들고 다녀야 할 때를 위한 보조 수단이다.

- 폴더째 옮기지 않아도 되고, 파일 앱에서 연 `file://` 페이지가 형제 파일 19개를
  읽어 줄지 걱정할 필요가 없다. 다만 **iOS 에서 `file://` 로 여는 경로 자체는
  여전히 미검증이다** — 확인한 것은 https 경로뿐이다
- 빌드 도구는 여전히 없다([1.3](#13-실행-환경)). 하는 일은 `<link>` / `<script src>` 를
  그 자리에 내용으로 바꿔 끼우는 것뿐이고 **순서는 원본 그대로**다
- 인라인한 코드 안의 `</script>` 는 이스케이프한다. 문자열이나 주석 속이어도
  파서는 거기서 스크립트를 끊는다
- **생성물이므로 직접 고치지 않는다.** 원본을 고치고 다시 빌드한다

> 실측(2026-07-30, Firefox 153): `worldmap-single.html` 은 같은 폴더의 `index.html` 과
> **같은 OPFS 를 공유한다.** `file://` 에서는 둘이 한 출처로 묶이므로, 데스크톱에서
> 둘을 오가도 데이터가 그대로다. 단 이건 데스크톱 파이어폭스에서 확인한 것이고
> **iOS 는 미검증**이다. 기기가 다르면 어차피 별개 저장소다([1.3](#13-실행-환경)).

---

## 4. 캔버스 동작 규칙

### 4.1 고스트 핀 (필수 기능)
연결 상대 노드가 **현재 캔버스에 배치되지 않은** 경우, 소켓 옆에 개수 배지를 표시한다.

```
[인물] ●───  레나
       ●  ↗3      ← 이 캔버스에 없는 연결 3건
```

배지 클릭 시 팝오버:
- 해당 노드 목록 표시 (이름 + 엣지 라벨)
- 항목별 **[이 캔버스로 가져오기]** / **[해당 노드가 있는 캔버스로 점프]**

이 기능이 없으면 "연결했는데 안 보인다" 문제로 툴이 붕괴한다.

### 4.2 삭제 2단계 분리

| 조작 | 동작 |
|---|---|
| `Delete` | **배치만 제거.** 노드·엣지는 DB에 그대로 |
| 우클릭 → 전역 삭제 | 노드 자체 삭제. 아래 경고 모달 필수 |

전역 삭제 모달 표기 내용:
> `chr_lena` 를 완전히 삭제합니다.
> · 참조 엣지 **7개** 가 함께 삭제됩니다
> · **3개** 캔버스에서 배치가 제거됩니다
> [영향 목록 보기] [취소] [삭제]

### 4.3 조작 키맵 (블루프린트 준용)

| 조작 | 키 |
|---|---|
| 팬 | 우클릭 드래그 / 스페이스+드래그 |
| 줌 | 휠 |
| 전체 보기 | `F` (선택 노드 포커스) / `Home` (전체 fit) |
| 노드 생성 | 빈 곳 우클릭 → 타입 선택 → 검색 |
| 소켓에서 빈 곳 드래그 후 놓기 | 타입 필터된 노드 검색 팝업 (블루프린트의 컨텍스트 감응) |
| 다중 선택 | 드래그 박스 / `Ctrl`+클릭 |
| 코멘트 박스 | 선택 후 `C` |
| 노드 접기 | `Alt`+클릭 |
| 검색 팔레트 | `Ctrl`+`P` |

### 4.4 터치 대응 (iOS)

위 키맵은 마우스+키보드 전용이라 iOS에서 대응 동작이 하나도 없다.
Phase 2에서 제스처 레이어를 별도로 얹는다.

| 조작 | 데스크톱 | 터치 |
|---|---|---|
| 팬 | 우클릭 드래그 | 1손가락 드래그 (빈 곳) |
| 줌 | 휠 | 핀치 |
| 컨텍스트 메뉴 | 우클릭 | 롱프레스 |
| 다중 선택 | 드래그 박스 / `Ctrl`+클릭 | 선택 모드 토글 후 탭 |
| 노드 접기 | `Alt`+클릭 | 노드 헤더 더블탭 |
| 검색 팔레트 | `Ctrl`+`P` | 상단 바 검색 버튼 |

- 소켓 히트박스는 터치에서 최소 44pt로 확대
- 표 뷰는 Phase 1부터 좁은 화면 대응 (iPhone에서도 열람 가능해야 함)

---

## 5. 노드 타입 정의

### 5.1 소켓의 2계층 구조

블루프린트의 실행핀/데이터핀 분리를 그대로 차용한다.

| 계층 | 명칭 | 색/형태 | 보유 타입 | 의미 |
|---|---|---|---|---|
| 1 | **인과핀** | 흰색 삼각형, 노드 좌우 | 사건만 | 원인 사건 → 결과 사건. 시나리오의 뼈대 |
| 2 | **관여핀** | 타입별 색 원형, 노드 하단 | 전체 | 누가·무엇이 관여했는가 |

→ **사건 = 함수 노드 / 나머지 = 변수 노드** 구조.
시나리오 흐름은 인과핀만 따라 좌→우로 읽으면 되고, 설정 정보는 아래에서 꽂힌다.

### 5.2 타입 목록

색상은 잠정값.

#### 사건 (event) — `#E8A33D`
| 소켓 | 허용 타입 | 다중 |
|---|---|---|
| 인과In | event | ✔ |
| 인과Out | event | ✔ |
| 인물 | character | ✔ |
| 조직 | organization | ✔ |
| 물건 | item | ✔ |
| 개념 | concept | ✔ |
| 장소 | location | ✔ |

필드: `시점`(→ 6장) / `요약`(1줄) / `상세`(마크다운) / `결과`

#### 인물 (character) — `#4FA8E8`
| 소켓 | 허용 타입 | 다중 |
|---|---|---|
| 관여 사건 | event | ✔ |
| 소속 | organization | ✔ |
| 소지·제작 | item | ✔ |
| 신념 | concept | ✔ |
| 인물관계 | character | ✔ (무방향) |
| 거점 | location | ✔ |

필드: `이명` / `생몰` / `소개` / `비밀` / `외형 메모`

#### 조직 (organization) — `#C75B5B`
| 소켓 | 허용 타입 | 다중 |
|---|---|---|
| 관여 사건 | event | ✔ |
| 상위 조직 | organization | ✔ |
| 하위 조직 | organization | ✔ |
| 구성원 | character | ✔ |
| 이념 | concept | ✔ |
| 보유물 | item | ✔ |
| 세력권 | location | ✔ |

필드: **`규모등급`** (패거리 / 조직 / 도시세력 / 국가 / 초국가) / `설립 시점` / `해체 시점` / `개요`

> 규모등급은 필터·시각적 크기 차별화에 사용. "동네 왈패부터 국가 단위까지"를 한 타입으로 처리하는 장치.

#### 물건·작품 (item) — `#7FBF6A`
| 소켓 | 허용 타입 | 다중 |
|---|---|---|
| 관여 사건 | event | ✔ |
| 소지자·제작자 | character, organization | ✔ |
| 담은 개념 | concept | ✔ |
| 소재 장소 | location | ✔ |

> 소유자와 제작자를 소켓으로 쪼개지 않는다. 성격은 엣지 라벨(`소유`/`제작`/`상실`/`갈망`)이
> 갖는다. 쪼개면 "제작자 소켓에 꽂혔는데 라벨은 소유"인 모순이 생기고, 무엇보다
> **`갈망`** 은 소유자도 제작자도 아니라 꽂을 자리가 없다.

필드: `분류`(무기 / 유물 / 서적 / 가면 / 악곡 / 기타) / `설명` / `내력`

#### 개념 (concept) — `#A87FD0`
| 소켓 | 허용 타입 | 다중 |
|---|---|---|
| 상위 개념 | concept | ✔ |
| 파생 개념 | concept | ✔ |
| 신봉자 | character, organization | ✔ |
| 전파 매체 | item | ✔ |
| 관여 사건 | event | ✔ |

필드: `분류`(사상 / 이론 / 종교 / 기술 / 미신) / `요지` / `상세`

> `기원`을 별도 소켓으로 두지 않는다. 사건 쪽의 `개념` 소켓과 짝이 어긋나기 때문이다.
> 한 쌍(`사건.개념 ↔ 개념.관여 사건`)으로 두고 `기원`/`관여`/`확산`/`억압`을 라벨로 구분한다.

#### 장소 (location) — `#8A8A8A`
| 소켓 | 허용 타입 | 다중 |
|---|---|---|
| 상위 장소 | location | ✔ |
| 하위 장소 | location | ✔ |
| 지배 조직 | organization | ✔ |

필드: `분류`(지역 / 도시 / 시설 / 이계) / `설명`

### 5.3 스키마 외부화
위 정의는 전부 `schema.json`에 데이터로 선언한다.
우측 패널·노드 렌더링·연결 유효성 검사는 모두 이 파일을 읽어 동작.
→ 나중에 `종족`, `기술`, `퀘스트` 타입을 추가할 때 **UI 코드를 수정하지 않는다.**

```json
{
  "types": {
    "character": {
      "label": "인물",
      "color": "#4FA8E8",
      "icon": "user",
      "sockets": [
        { "key": "orgs", "label": "소속", "accepts": ["organization"],
          "multi": true, "labelPreset": "org_role" }
      ],
      "fields": [
        { "key": "alias", "label": "이명", "widget": "text" },
        { "key": "secret", "label": "비밀", "widget": "markdown", "reveal": "spoiler" }
      ]
    }
  }
}
```

#### 5.3.1 스키마 편집기

**앱 안에 편집기가 있어야 한다.** 폴더 저장(Chrome)에서는 `data/schema.json` 을 탐색기로
열면 되지만, **OPFS(Firefox·iOS)에서는 그 파일이 아예 안 보인다**([1.3](#13-실행-환경)).
편집기가 없으면 그 환경에서는 타입을 하나도 늘릴 수 없어 5.3 의 목표가 성립하지 않는다.

상단 바 **[스키마]** → 타입 목록. 각 타입의 소켓·필드·노드 수를 함께 보여준다.

| 조작 | 범위 |
|---|---|
| 타입 추가 | 표시명·키·ID 접두사·색 + **연결할 상대 타입** |
| 타입 편집 | 표시명·색·ID 접두사 |
| 타입 삭제 | 그 타입만 받던 상대 소켓도 함께 정리 |
| JSON 직접 편집 | 소켓·필드·라벨 프리셋까지 전부 |
| 내보내기 / 불러오기 / 기본값 | 스키마 파일 단독 |

- **타입 추가는 양쪽 소켓을 같이 만든다.** 한쪽만 만들면 패널에서는 이어지는 것처럼
  보이는데 캔버스에서 선이 안 나온다. 그 사실을 나중에 발견하게 두면 원인을 못 찾는다
- 타입 **키는 변경 불가**. 노드가 `type` 으로 자기를 가리킨다 ([2.2](#22-id-규칙) 와 같은 이유)

#### 5.3.2 적용 전 두 가지를 따로 본다

스키마는 데이터지만 아무 데이터나 아니다. 여기가 깨지면 표·패널·캔버스가 같이 깨진다.

**① 스키마 자체가 말이 되는가** (`validate` — 기존 데이터와 무관)

| 오류 (적용 거부) | 경고 (적용은 됨) |
|---|---|
| `idPrefix` 형식·중복 | 없는 `labelPreset` 참조 (generic 으로 대체) |
| 소켓/필드 키 중복 | `reciprocal` 이 가리키는 소켓 없음 |
| 없는 타입을 `accepts` | 모르는 `widget` |
| **짝 없는 소켓** — 아래 참조 | `cardFields` 오타 |
| `select` 인데 `options` 없음 | `whenPrecision` 에 `unknown` 없음 |
| `flags.status/reveal/impl` 누락, `default` 불일치 | |

> **짝 없는 소켓이 이 스키마의 핵심 불변식이다.** A 가 B 를 받으면 B 도 A 를 받아야
> 한다. 캔버스 연결선은 양 끝에 핀이 있어야 그릴 수 있기 때문이다([12장](#12-미결정-사항)).
> 한쪽만 있으면 조용히 선만 사라지므로 **오류로 막는다.**

**② 지금 쌓인 데이터에 무슨 일이 생기는가** (`impact`)

적용 전에 개수를 세어 보여주고 확인을 받는다. 가져오기([3.4](#34-교환-포맷))와 같은 태도다.

- 타입 삭제 → 그 타입 노드 N개가 화면에서 사라짐
- 소켓 삭제 → 물려 있던 엣지 N건이 사라짐
- 필드 삭제 → 값이 든 노드 N개
- 필수 추가 → 미입력 노드 N개가 검증 경고로
- `idPrefix` 변경 → 기존 ID는 그대로 (ID 는 변경 불가)

**적용해도 노드·엣지 데이터는 건드리지 않는다.** 사라진 필드의 값도 `nodes.json` 에
남긴다. 되돌리고 싶을 때 값이 이미 지워져 있으면 방법이 없다. 적용 직전 백업 스냅샷을 남긴다.

---

## 6. 시점 필드 설계

### 6.1 「가면을 벗다」의 달력

**1년 = 7달 = 199일. 윤달 없음.** 위성 세 개의 위치와 형태로 정해진 시간표라
달 길이가 균등하지 않다. 행성의 공전 주기와 정확히 맞아떨어져 보정이 필요 없다.

| # | 달 | 일수 | 연중 시작일 |
|---:|---|---:|---:|
| 1 | 이슬의 달 | 24 | 1 |
| 2 | 비의 달 | 16 | 25 |
| 3 | 하늘의 달 | 48 | 41 |
| 4 | 룬의 달 | 31 | 89 |
| 5 | 바람의 달 | 28 | 120 |
| 6 | 삼위일체의 달 | 8 | 148 |
| 7 | 하나의 달 | 44 | 156 |
| | **합계** | **199** | |

이 표는 `schema.json` 의 `calendar` 에 데이터로 있다. **`calendar.js` 는 숫자를 갖고
있지 않다** — 달 이름이나 일수를 고쳐도 코드는 손대지 않는다([5.3](#53-스키마-외부화)).

> 199 는 소수라 어떤 주기로도 정수로 나뉘지 않는다. 달 길이가 들쭉날쭉한 것과 함께,
> 균등하게 쪼갤 수 없는 시간표라는 설정과 앞뒤가 맞는다.

### 6.2 sort 산출 규칙

가상 연대는 `Date` 파싱이 불가능하다. 3필드 구조로 처리한다.

```json
"when": {
  "sort": 981.2563,
  "display": "981년 하늘의 달 12일",
  "precision": "exact"
}
```

```
sort = 연 + (연중일 - 1) / 199
```

| 필드 | 역할 |
|---|---|
| `sort` | 정렬 전용 실수. **정수부가 연, 소수부가 연중 위치.** 이게 원본이다 |
| `display` | 화면 표기. 연·달·일에서 자동 생성하되 손으로 덮어쓸 수 있다 |
| `precision` | `exact` / `month` / `year` / `decade` / `unknown` |

- **연·달·일을 따로 저장하지 않는다.** `sort` 에서 역산되므로 두 곳에 같은 사실을
  두면 어긋날 자리만 생긴다
- `precision` 이 뭉개진 값은 그 구간의 **시작점**을 `sort` 로 삼는다.
  막대는 거기서 오른쪽으로 뻗는다
- **기(era)는 쓰지 않는다** (2026-07-30 결정). 연도만으로 표기한다
- `unknown` 은 타임라인 하단 "시점 미정" 트레이에 격리
- 인과핀 연결과 `sort` 가 모순되면(결과가 원인보다 앞) 검증 오류([10장](#10-검증-규칙))

### 6.3 막대 폭 — 달은 고정값을 쓸 수 없다

```json
{ "key": "year",  "label": "연", "timeline": "range", "span": 1 }
{ "key": "month", "label": "달", "timeline": "range", "spanFrom": "month" }
```

| precision | 폭(연 단위) |
|---|---|
| `exact` | 0 — 점으로 |
| `month` | **그 달의 일수 / 199** — `sort` 에서 어느 달인지 역산해 잰다 |
| `year` | 1 |
| `decade` | 10 |

> 삼위일체의 달(8일)과 하늘의 달(48일)은 **6배** 차이다. 고정 `span` 하나로 뭉뚱그리면
> 막대가 나타내는 불확실성이 거짓말이 된다. 그래서 `spanFrom: "month"` 는
> "숫자를 쓰지 말고 달력에서 재라"는 뜻이다.

타임라인 눈금은 기본이 연이고, 한 해가 340px 이상으로 넓어지면 달 경계로 바뀐다.
눈금 간격이 제각각인데, 그게 이 세계의 달력이 실제로 생긴 모양이다.
기본 배율은 데이터 전체가 보이도록 자동으로 맞춘다([맞춤] 버튼으로 되돌린다).

---

## 7. 상태 플래그

모든 노드·엣지가 공통으로 갖는 3종 플래그. 노드 테두리 스타일로 즉시 구분한다.

| 플래그 | 값 | 시각 표현 |
|---|---|---|
| `status` | `confirmed` 확정 / `draft` 가설 / `dropped` 폐기 | 실선 / 점선 / 반투명+취소선 |
| `reveal` | `public` 공개 / `spoiler` 미공개 | 우상단 자물쇠 아이콘 |
| `impl` | `done` 구현됨 / `todo` 미구현 / `na` 해당없음 | 좌하단 체크 배지 |

활용:
- `impl:todo` 필터 → **설정만 있고 게임에 안 들어간 것** 일괄 확인
- `reveal:spoiler` 숨김 토글 → 방송/스크린샷용 안전 모드
- `status:dropped` 는 기본 숨김. 지우지 않고 남겨두는 폐기 설정 보관소

---

## 8. 엣지 라벨 프리셋

엣지는 단순 연결이 아니라 **관계의 의미**를 담는다. 클릭 시 우측 패널에서 편집.

| 소켓 조합 | 프리셋 | 추가 필드 |
|---|---|---|
| 인물 → 사건 | 주도 / 가담 / 목격 / 피해 / 무관 | `note` |
| 조직 → 사건 | 주도 / 관여 / 대립 / 피해 | `note` |
| 인물 → 조직 | (자유) | `직위`, `기간` |
| 인물 ↔ 인물 | 스승 / 제자 / 혈연 / 원수 / 연인 / 동료 (자유 입력 허용) | `note`, `방향성` |
| 인물 → 물건 | 소유 / 제작 / 상실 / 갈망 | `기간` |
| 사건 → 사건 | 직접원인 / 간접원인 / 계기 / 저지 | `note` |
| 인물 → 개념 | 신봉 / 창시 / 배격 / 이용 | `note` |
| 사건 → 개념 | 기원 / 관여 / 확산 / 억압 | `note` |

프리셋도 `schema.json`의 `labelPreset` 로 관리.

---

## 9. 뷰 모드

| 뷰 | 용도 | 우선도 |
|---|---|---|
| **표 뷰** | 타입별 목록·일괄 편집·필드 채우기. 데이터 입력의 주 창구 | 1 |
| **그래프 뷰** | 캔버스. 관계 파악·시나리오 설계 | 2 |
| **포커스 뷰** | 선택 노드 기준 N단계(1~3) 이웃만 자동 배치해 임시 표시. **규모 대응의 핵심** | 3 |
| **타임라인 뷰** | 사건을 `sort` 순 가로 배치. 인물 레인별 필터 가능 | 3 |

> 노드 100개를 넘기면 캔버스는 읽을 수 없게 된다.
> 검색보다 포커스 뷰가 실전에서 훨씬 유용하다.

### 9.1 화면 배치

`#main` 은 CSS 그리드 한 줄이고, 열 폭은 **CSS 변수 세 개가 전부 결정한다.**
`layout.js` 는 그 숫자만 바꾼다. 레이아웃 규칙 자체는 `style.css` 밖으로 새지 않는다.

```
표 뷰      [ 타입바 ][│][      가운데      ][│][ 상세 ]
                --tabs-w        1fr           --panel-w
그래프/타임라인       [      가운데      ][│][ 상세 ]
```

| 뷰 | 타입바 | 이유 |
|---|---|---|
| 표 | 보임 | 타입 전환이 표 뷰의 주 조작이다 |
| 그래프 · 타임라인 | 숨김 | 여기서는 쓰지 않는데 폭만 먹는다 |

- 경계의 `│` 가 **드래그 스플리터**. 더블클릭(또는 `Home`)으로 기본값 복귀,
  포커스 후 `←` `→` 로 16px 씩
- 하한 — 타입바 120px / 상세 260px / **가운데 320px**.
  가운데 하한은 다른 둘의 상한을 동적으로 깎는다. 가운데가 뭉개지면 어느 뷰도 못 읽는다
- 폭은 `localStorage` 에 남긴다. **번들([3.4](#34-교환-포맷))에는 넣지 않는다** —
  세계관 데이터가 아니라 이 기기에서의 보기 설정이고, 화면 크기가 다른 기기로
  따라가면 오히려 방해가 된다
- 좁은 화면(≤860px)에서는 세로로 쌓고 상세는 서랍으로 덮으므로 스플리터를 숨긴다

> **뷰 전환은 `pane.hidden` 으로 한다. 그런데 `#id { display: flex }` 는 UA 의
> `[hidden] { display: none }` 을 이긴다.** 그래서 pane 마다
> `#…-pane[hidden] { display: none }` 이 반드시 짝으로 있어야 한다.
> 하나라도 빠지면 그 pane 이 안 숨고 그리드 칸을 계속 차지해, 보이는 자식 수가
> 열 수를 넘겨 **레이아웃이 다음 줄로 밀린다.**

> `body` 의 열은 `minmax(0, 1fr)` 이어야 한다. 그냥 `1fr` 이면 열이 자식의
> min-content 까지 늘어나, 좁은 화면에서 상단 바가 페이지 전체를 밀어낸다.

---

## 10. 검증 규칙

수동 실행 + 저장 시 백그라운드 체크. 결과는 하단 패널에 목록으로.

| 항목 | 수준 |
|---|---|
| 고아 노드 (엣지 0개) | 경고 |
| 필수 필드 미입력 | 경고 |
| 인과 순환 (A→B→A) | 오류 |
| 인과 방향과 `sort` 값 모순 | 오류 |
| 어떤 캔버스에도 배치되지 않은 노드 | 정보 |
| 중복 의심 (표시명 유사도 높음) | 정보 |
| `status:draft` 가 `confirmed` 노드의 근거로 쓰임 | 정보 |

---

## 11. 구현 로드맵

### Phase 1 — 데이터 기반 ✅ 구현 완료
- `schema.json` 로더 (`file://` 에서 fetch가 막히므로 JS에 내장 후 폴더에 써넣는다)
- **저장 어댑터** (`FsaAdapter` / `OpfsAdapter` / `MemoryAdapter`) + 환경 자동 선택
- 연결 / 자동저장 / 백업 로테이션
- **번들 내보내기·가져오기 + 병합 UI** ([3.4](#34-교환-포맷))
- 노드 CRUD
- **표 뷰** + 우측 패널 (스키마 구동 렌더링), 좁은 화면 대응
- **스키마 편집기** ([5.3.1](#531-스키마-편집기)) — OPFS 환경에서는 이게 없으면 타입을 못 늘린다
- 엣지 생성은 우측 패널의 소켓 목록에서 노드 검색 방식으로만

> 캔버스보다 표 뷰를 먼저 만든다.
> 노드가 30개도 안 쌓인 상태에서 그래프 UI를 만들면 무엇이 불편한지 알 수 없다.

### Phase 2 — 캔버스 ✅ 구현 완료
- 팬/줌, 노드 배치, 베지어 연결선
- 소켓 드래그 연결 + 타입 검사
- **고스트 핀**
- 삭제 2단계, 코멘트 박스, 노드 접기
- 다중 캔버스 탭
- 터치 제스처 ([4.4](#44-터치-대응-ios))

> 핀 좌표는 DOM을 재지 않고 고정 메트릭(`M.W/HEAD/ROW/PAD_B`)으로 계산한다.
> 팬·줌·드래그마다 `getBoundingClientRect` 를 부르면 노드 80개에서 바로 버벅인다.
> **`style.css` 의 `.cv-node__head` height, `.cv-row` height, `.cv-node__rows` padding 은
> `canvas.js` 의 `M` 과 반드시 일치해야 한다.** 어긋나면 선이 핀에서 떨어진다.

### Phase 3 — 뷰/검증 ✅ 구현 완료
- 포커스 뷰 — 캔버스 렌더러를 **저장되지 않는 임시 배치** 위에서 재사용한다.
  1~3단계를 동심원으로 자동 배치하고, 2단계 이상은 접어서 화면을 아낀다
- 타임라인 뷰
- 검증 패널 — 수동 실행 + 저장 뒤 백그라운드
- 필터·태그·전역 검색 (`Ctrl`+`P`)
- `reveal` 안전 모드 토글

### Phase 4
- **마크다운 문서 일괄 내보내기** ✅ 구현 완료 ([3.5](#35-마크다운-내보내기))
- 게임 데이터 export — **보류** ([12장](#12-미결정-사항))
- 이미지 첨부 — **보류** ([12장](#12-미결정-사항))

> 나머지 둘은 코드가 아니라 결정을 기다린다. **지금 코드로 할 수 있는 Phase 4 작업은
> 없다.** 남은 실작업은 12장의 `sort` 산출 규칙이 풀려야 열린다.

---

## 12. 미결정 사항

- [x] ~~`sort` 값 산출 규칙~~ → **2026-07-30 확정.** 1년 7달 199일, 윤달 없음.
      `sort = 연 + (연중일-1)/199`, 기(era)는 쓰지 않는다 ([6장](#6-시점-필드-설계))
- [ ] 물건 `분류` 항목이 실제 게임 아이템 분류와 일치할지
- [x] ~~인물관계 무방향 처리 방식~~ → **엣지 1개 + `방향성` 필드**.
      3.2의 "역방향은 저장하지 않는다" 원칙과 일관되게
- [ ] 게임 데이터 export 포맷 — **2026-07-30 보류.** 「가면을 벗다」가 데이터를 어떤
      형태로 읽는지가 정해져야 시작할 수 있다. 그때까지 준비물([2.3](#23-게임-연동-대비))만
      유지한다
- [ ] 이미지·참고자료 첨부 여부 — **2026-07-30 보류.** 채택하더라도 SPEC 이 적어둔
      "경로 참조 방식" 은 OPFS 에서 성립하지 않으므로([1.3](#13-실행-환경)) 저장 방식부터
      다시 정해야 한다
- [x] ~~**iOS 서빙 위치**~~ → **2026-07-30 확정.** GitHub Pages,
      `https://isorbgit.github.io/story-tool/` ([1.5](#15-배포)).
      아이패드에서 열기·편집·저장·불러오기까지 실측 완료.
      LAN 서버 안은 폐기 — 평문 http 라 OPFS 가 막힌다
- [x] ~~인물 `소지·제작` ↔ 물건 `소유자`/`제작자` 불일치~~ → **물건 쪽을 `소지자·제작자`
      하나로 합침.** 성격은 라벨이 갖는다
- [x] ~~5.2 표에 없는 `관여 사건` 소켓~~ → **확정.** 캔버스 연결선은 양쪽 끝에 핀이
      있어야 그릴 수 있다
- [x] ~~개념 `기원` ↔ 사건 `개념` 의미 불일치~~ → **한 쌍으로 합치고 `concept_event`
      프리셋으로 구분**
