# 세계관 데이터 — 공개 저장소에 올리지 않는다.
# 이 폴더를 저장 위치로 고르면 어댑터가 여기에 만든다.
/data/
/backup/

# 빌드 결과물. build.cmd 로 언제든 다시 만든다.
/worldmap-single.html

# 로컬 도구 설정
/.claude/

# 설계 문서 — 작품 설정 예시가 들어 있어 기본적으로 제외한다.
# 공개해도 괜찮다면 이 줄을 지우면 된다.
/SPEC.md

# OS 잡파일
Thumbs.db
desktop.ini
.DS_Store
