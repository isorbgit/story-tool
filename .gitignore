# 세계관 데이터 — 공개 저장소에 올리지 않는다.
# 이 폴더를 저장 위치로 고르면 어댑터가 여기에 만든다.
/data/
/backup/

# 원본 설정 문서와 변환 중간 산출물. 데이터와 같은 이유로 올리지 않는다.
*.pptx
*.wmap.json
/import/

# 빌드 결과물. build.cmd 로 언제든 다시 만든다.
/worldmap-single.html

# 로컬 도구 설정
/.claude/

# OS 잡파일
Thumbs.db
desktop.ini
.DS_Store
